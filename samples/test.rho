// A simple contract

new helloworld in {
  contract helloWorld (name) = {
    "hello, ".display(name, "!\n")
  } |
  helloworld("Joe")
}

/*
new a in {
  contract b (c) = {
    for (d <- c) {
      match d with
        true => {
          "hello".display() |
        }
    }
  }
} */
